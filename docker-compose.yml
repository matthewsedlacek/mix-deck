services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mixdeck
      POSTGRES_PASSWORD: mixdeck
      POSTGRES_DB: mixdeck
    ports:
      - "5432:5432"
    volumes:
      - mixdeck_pgdata:/var/lib/postgresql/data

volumes:
  mixdeck_pgdata:
